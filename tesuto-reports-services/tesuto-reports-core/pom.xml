<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.cccnext.tesuto</groupId>
		<artifactId>tesuto-reports-services</artifactId>
		<version>2.2.0-SNAPSHOT</version>
	</parent>
	<artifactId>tesuto-reports-core</artifactId>
	<name>tesuto-report-core</name>
	<description>Module for creating reports for tesuto admins</description>
	<properties>
		<flyway.table>schema_version_reports_5</flyway.table>
		<flyway.baseline>0.0</flyway.baseline>
		<jar.name>tesuto-reports-core</jar.name>
	</properties>

	<dependencies>
	
	    <dependency>
			<groupId>org.cccnext.tesuto</groupId>
			<artifactId>tesuto-activation-api</artifactId>
			<version>${tesuto.version}</version>
		</dependency>
		<dependency>
			<groupId>org.cccnext.tesuto</groupId>
			<artifactId>tesuto-delivery-core</artifactId>
			<version>${tesuto.version}</version>
		</dependency>
		<dependency>
			<groupId>org.cccnext.tesuto</groupId>
			<artifactId>tesuto-content-api</artifactId>
			<version>${tesuto.version}</version>
		</dependency>
		<dependency>
            <groupId>org.cccnext.tesuto</groupId>
            <artifactId>tesuto-activation-dao-postgresql</artifactId>
            <version>${tesuto.version}</version>
        </dependency>
        <dependency>
            <groupId>org.cccnext.tesuto</groupId>
            <artifactId>tesuto-admin-api</artifactId>
            <version>${tesuto.version}</version>
        </dependency>
        <dependency>
            <groupId>org.cccnext.tesuto</groupId>
            <artifactId>tesuto-qti-importer-core</artifactId>
            <version>${tesuto.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.cccnext.tesuto</groupId>
            <artifactId>tesuto-test-dependencies</artifactId>
            <version>${tesuto.version}</version>
            <scope>test</scope>
        </dependency>
	</dependencies>
	<profiles>
		<profile>
			<id>database</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.flywaydb</groupId>
						<artifactId>flyway-maven-plugin</artifactId>
						<version>${flyway-version}</version>
						<configuration>
							<url>${env.JDBC_URL_REPORTS}</url>
							<user>${env.JDBC_USER_REPORTS}</user>
							<password>${env.JDBC_PASSWORD_REPORTS}</password>
							<outOfOrder>true</outOfOrder>
							<locations>
								<location>${flyway.migration.location}</location>
							</locations>
							<baselineOnMigrate>${env.baseline.on.migrate}</baselineOnMigrate>
							<baselineVersion>${flyway.baseline}</baselineVersion>
							<table>${flyway.table}</table>
							<ignoreMissingMigrations>true</ignoreMissingMigrations>
						</configuration>
						<executions>
							<execution>
								<phase>process-resources</phase>
								<goals>
									<goal>migrate</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
